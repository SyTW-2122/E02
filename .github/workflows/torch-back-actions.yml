name: Backend actions for CI/CD
on:
  push:
    branches: [ main ]

jobs:
  test:
    runs-on: [ubuntu-latest]
    steps:
      - uses: actions/checkout@v2
      - run: cd torch-back
      - run: npm install
      - run: npm test

  #build:
  #deploy: